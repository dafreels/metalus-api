import { TreeonloadDirective } from './treeonload.directive';

describe('TreeonloadDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeonloadDirective();
    expect(directive).toBeTruthy();
  });
});
